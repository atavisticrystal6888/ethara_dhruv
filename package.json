{
  "name": "fswa-team-task-manager",
  "private": true,
  "version": "0.1.0",
  "description": "Full-stack Team Task Manager assignment with React, Express, PostgreSQL, SQL migrations, and Railway deployment notes.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "db:seed": "npm run db:seed --workspace backend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "test": "npm run test --workspace backend && npm run test --workspace frontend",
    "test:e2e": "npm run test:e2e --workspace frontend",
    "lint": "npm run lint --workspace backend && npm run lint --workspace frontend",
    "verify:submission": "node scripts/verify-submission.mjs"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
